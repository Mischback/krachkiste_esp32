# do not track tox's working directory
.tox/

# do not track the repository's internal make stamps
.make-stamps/

# do not track the actual configuration, as it may include private settings
sdkconfig
sdkconfig.old

# obviously don't track the build directory
build/

# do not track generated documentation (and intermediate files)
docs/build/
docs/source/doxygen/

# just for internal use and learning...
build_stats.log
